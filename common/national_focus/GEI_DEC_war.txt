focus_tree = { 
	id = GEI_DEC_war_focus_tree
	continuous_focus_position = { x = 4000 y = 200 } 
#	shared_focus = BA_new_world_technology
	country = {
		factor = 0
		modifier = {
			add = 10
			tag = GEI
		}
	}


	focus = { # 최후통첩
		id = GEI_DEC_last_messeage
		icon = GFX_GEI_DEC_last_messeage

		x = 1
		y = 2
        relative_position_id = GEI_DEC_zero_clock

		cost = 1
        prerequisite = { focus = GEI_DEC_control }
		prerequisite = { focus = GEI_DEC_gathering }
		prerequisite = { focus = GEI_DEC_replenishment }
		text_icon = The_New_Order_focus_style
		

		available = { }
		search_filters = {
			
						FOCUS_FILTER_ANNEXATION		
		}
		completion_reward = {
			declare_war_on = {
                target = BLM
                type = annex_everything
            }
			news_event = { id = BA_news.149 hours = 72 }
		}
	}

	focus = { # 정화
		id = GEI_DEC_cleanse
		icon = GFX_GEI_DEC_cleanse

		x = -1
		y = 3
        relative_position_id = GEI_DEC_zero_clock

		cost = 1
        prerequisite = { focus = GEI_DEC_last_messeage }
		text_icon = The_New_Order_focus_style
		

		available = { }
		search_filters = {
			
						FOCUS_FILTER_ANNEXATION		
		}
		completion_reward = {
			GEH = {
				country_event = GEI_DEC_event.31
			}
			GEM = {
				country_event = GEI_DEC_event.31
			}
		}
	}

	focus = { # 에덴
		id = GEI_DEC_eden
		icon = GFX_GEI_DEC_eden

		x = 1
		y = 3
        relative_position_id = GEI_DEC_zero_clock

		cost = 1
        prerequisite = { focus = GEI_DEC_last_messeage }
		text_icon = The_New_Order_focus_style
		

		available = { }
		search_filters = {
			
						FOCUS_FILTER_ANNEXATION		
		}
		completion_reward = {
			TRM = {
				country_event = GEI_DEC_event.31
			}
		}
	}

	focus = { # 에덴
		id = GEI_DEC_red_star
		icon = GFX_GEI_DEC_red_star

		x = 3
		y = 3
        relative_position_id = GEI_DEC_zero_clock

		cost = 1
        prerequisite = { focus = GEI_DEC_last_messeage }
		text_icon = The_New_Order_focus_style
		

		available = { }
		search_filters = {
			
						FOCUS_FILTER_ANNEXATION		
		}
		completion_reward = {
			RED = {
				country_event = GEI_DEC_event.31
			}
		}
	}

	focus = { # 최후통첩
		id = GEI_DEC_last_attack
		icon = GFX_GEI_DEC_good_bye_night

		x = 1
		y = 4
        relative_position_id = GEI_DEC_zero_clock

		cost = 1
        prerequisite = { focus = GEI_DEC_eden }
		prerequisite = { focus = GEI_DEC_red_star}
		prerequisite = { focus = GEI_DEC_cleanse }
		text_icon = The_New_Order_focus_style
		

		available = { }
		search_filters = {
			
						FOCUS_FILTER_ANNEXATION		
		}
		completion_reward = {
			declare_war_on = {
                target = GEM
                type = annex_everything
            }
			declare_war_on = {
                target = TRM
                type = annex_everything
            }
			declare_war_on = {
                target = RED
                type = annex_everything
            }
			declare_war_on = {
                target = HEU
                type = annex_everything
            }
			if = {
                limit = {
                    NOT = { is_in_faction_with = GEH } # 게헨나 선도부와 팩션이 달라야 함
                    country_exists = GEH # 게헨나 선도부가 있어야만 함
                }
                declare_war_on = {
                       target = GEH # 게헨나 선도부에 전쟁 명분 줌
                       type = annex_everything
                   }
            }
			hidden_effect = {
				country_event = { id = GEI_DEC_event.37 days = 200}
			}
			news_event = { id = BA_news.150 hours = 8 }
		}
	}

	focus = { # 불리일체의하늘
		id = GEI_DEC_sky_unity
		icon = GFX_GEI_DEC_sky_unity

		x = 1
		y = 5
        relative_position_id = GEI_DEC_zero_clock

		cost = 1
        prerequisite = { focus = GEI_DEC_last_attack }
		text_icon = The_New_Order_focus_style
		

		available = {
			TRM = { exists = no }
			GEM = { exists = no }
			GEH = { exists = no }
			RED = { exists = no }
			HEU = { exists = no }
			ABY = { exists = no }
			HYA = { exists = no }
			SHA = { exists = no }
		}
		search_filters = {
			
						FOCUS_FILTER_ANNEXATION		
		}
		completion_reward = {
			load_focus_tree = GEI_DEC_after_focus_tree
		}
	}
}

