IDC_surrender_of_france = { #surrender_of_france
	IDC_break_the_law = {
		icon = GFX_decision_hol_war_on_pacifism
		
		available = {
			has_country_flag = has_IDC_independence_war_starter
			VIN = {
				OR = {
					exists = no
					NOT = { is_subject_of = FRA }
				}
			}
			LAO = {
				OR = {
					exists = no
					NOT = { is_subject_of = FRA }
				}
			}
			CAM = {
				OR = {
					exists = no
					NOT = { is_subject_of = FRA }
				}
			}
			custom_trigger_tooltip = {
				tooltip = IDC_break_the_law_tt
				NOT = {
					any_state = {
						OR = {
							BA_vietnam_is = yes
							BA_cambodia_is = yes
							BA_laos_is = yes
						}
						is_controlled_by = FRA
					}
				}
			}
		}
		ai_will_do = {
            factor = 400
        }
		days_remove = IDC_break_the_law_cost_var
		cancel_trigger = {
			OR = {
				VIN = {
					is_subject_of = FRA
				}
				LAO = {
					is_subject_of = FRA
				}
				CAM = {
					is_subject_of = FRA
				}
				NOT = {
					custom_trigger_tooltip = {
						tooltip = IDC_break_the_law_tt
						NOT = {
							any_state = {
								OR = {
									BA_vietnam_is = yes
									BA_cambodia_is = yes
									BA_laos_is = yes
								}
								is_controlled_by = FRA
							}
						}
					}
				}
			}
		}
		cancel_effect = {
		}
		complete_effect = {
		}
		remove_effect = {
			clr_global_flag = IDC_independence_war_ongoing
			set_global_flag = IDC_independence_war_onended
			if = {
				limit = {
					CAM = { controls_state = 1068 }
				}
				CAM = { transfer_state = 1068 }
			}
			else_if = {
				limit = {
					VIN = { controls_state = 1068 }
				}
				VIN = { transfer_state = 1068 }
			}
			every_country = {
				limit = {
					OR = {
						has_country_flag = has_IDC_independence_war_starter
						has_country_flag = has_IDC_independence_war_controller
					}
					has_war_with = FRA
				}
				FRA = {
					white_peace = PREV
					if = {
						limit = { is_in_faction = yes }
						every_allied_country = {
							white_peace = PREV.PREV
						}
					}
				}
			}
		}
	}
}