BA_highlander = { #하이랜더 철도학원
	BA_invest_highlander = { #하이랜더 투자
		days_remove = 75
		available = {
			NOT = {has_country_flag = BA_nationalized_highlander}
		}
		cancel_trigger = {
			NOT = {
				has_idea = BA_highlander_academy_generic
			}
		}
		modifier = {
			consumer_goods_factor = 0.10
		}
		fixed_random_seed = yes
		icon = GFX_decision_highlander
		complete_effect = {
			custom_effect_tooltip = BA_highlander_invest_random_tt
			hidden_effect = {
				country_event = Highlander.0
				country_event = Highlander.0
			}
		}
		remove_effect = {
			hidden_effect = {
				country_event = {
					id = Highlander.2
				}
				country_event = {
					id = Highlander.8
					days = 20
				}
			}
		}
		ai_will_do = {
			base = 40

			### 이런 상황에서 하이랜더 투자를 더 열심히 한다! ###
			modifier = {
				is_major = yes 
				# 메이저 국가일 때 투자 확률 증가
				add = 20
			}

			### 이런 상황일 때엔 투자를 줄인다 ###
			modifier = {
				has_war = yes
				# 전쟁 중에 소비재 쓰지 말자
				factor = 0.5 
			}
			modifier = {
				has_defensive_war = yes
				# 특히 침략받고 있을 때에는 더더욱 쓰지 말자
				factor = 0.5 
			}
			modifier = {
				surrender_progress > 0.1
				# 자기 나라가 박살나고 있을 때 소비재 바치지 말자
				factor = 0.1
			}
			modifier = {
				OR = {
					tag = GEH
					tag = GEM
				}
				NOT = { has_country_flag = GEH_unified_gehenna_flag }
				# 게헨나는 나라 토막나서 민공도 없는데 어디에 낭비하니
				factor = 0 
			}
			modifier = {
				tag = HEU
				NOT = { has_country_flag = GEH_unified_gehenna_flag }
				NOT = { has_completed_focus = HEU_GSC_president_rikuhachima }
				
				# 룩셈부르크는 게헨나 정복 or 저지대 통합 이전에는 하이랜더 투자하지 않음
				factor = 0 
			}
			modifier = {
				num_of_civilian_factories < 25
				# 민공 없을 때 소비재 쓰지 않기
				factor = 0.1
			}
			modifier = {
				tag = TRM
				add = -10
			}
		}
		priority = 100
	}
	BA_press_highlander = { #하이랜더 압박
		icon = GFX_decision_oppression
		days_remove = 100
		visible = {
			controls_state = 6
			has_idea = BA_highlander_academy_generic
		}
		cost = 5
		complete_effect = {
			if = {
				limit = { NOT = { has_country_flag = BA_nationalized_highlander }}
				custom_effect_tooltip = BA_highlander_invest_small_tt
				add_to_variable = {
					BA_highlander_invest = 2
				}
			}
		}
	}
	BA_highlander_hide_list = { #목록 숨기기
		icon = GFX_decision_generic_train
		priority = -100
		days_re_enable = 5
		visible = {
			has_idea = BA_highlander_academy_generic
			NOT = {
				has_country_flag = BA_highlander_hide_list_flag
			}
		}
		remove_effect = {
			set_country_flag = BA_highlander_hide_list_flag
		}
	}
	BA_highlander_show_list = { #목록 보이기
		icon = GFX_decision_generic_train
		priority = -100
		days_re_enable = 5
		visible = {
			has_idea = BA_highlander_academy_generic
			has_country_flag = BA_highlander_hide_list_flag
		}
		remove_effect = {
			clr_country_flag = BA_highlander_hide_list_flag
		}
	}
}