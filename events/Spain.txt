add_namespace = spain
# 바닐라의 다른 파일에서 호출되는 이벤트는 여기 작성되어야 합니다.
# LIST:
#(9) 선거 온액션, 히든으로 변경 후 우리 모드 이벤트로 조정

# Election of 1936
country_event = {
	id = spain.9
	title = spain.9.t
	desc = spain.9.d
	picture = GFX_report_event_election_vote
	hidden = yes

	is_triggered_only = yes

	fire_only_once = yes

	trigger = {
		tag = SPR
		date < 1937.1.1
		NOT = {
			has_dlc = "La Resistance"
		}
	}

	immediate = {
		country_event = SPBA_event.1
	}

	# Popular Front
	option = {
		name = spain.9.a
	}
}